[aliases]
test=pytest

[tool:pytest]
addopts = --verbose --doctest-modules --cov=shennong --cov-report=html --cov-report=term:skip-covered
testpaths = test shennong
python_files = test/*.py

[build_sphinx]
source-dir = doc/source
build-dir = doc/build
